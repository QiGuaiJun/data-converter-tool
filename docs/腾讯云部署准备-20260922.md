# 腾讯云部署准备清单（2026-09-22）

> 目标：把 data-converter-tool 部署到轻量服务器 `81.70.230.116`，应用与本机 MySQL 同机运行。
> 配套文档：`docs/登录功能-实施设计-20260922.md` 第十二节。
> **本文不含任何真实密码**——密码通过环境变量或 `.env` 注入，不入库、不入 git。

---

## 一、你需要做的三件事（其余我来做）

| # | 事项 | 具体操作 | 为什么需要 |
|---|---|---|---|
| 1 | **开通服务器命令执行权限** | 见第二节 | 当前 `execute_command` 返回 AccessDeny，我无法远程装环境 |
| 2 | **决定 HTTPS 方案** | 见第三节 | 无域名时只有三种选择，各有代价 |
| 3 | **确认服务器上的 MySQL 是否已安装** | 见第四节 | 若未安装，需要在部署时一并装（约 2 分钟） |

> 密码类信息请**不要贴在对话里**（会留在记录中）。部署时用 `ssh` 交互输入，或写进服务器上的 `/opt/dc/.env`（权限 600）。
> 如果已经在对话里发过数据库密码，**部署完成后请立即改一次**。

---

## 二、命令执行权限怎么配（回答你的第 3 问）

### 方案 A：开腾讯云 API 权限（推荐，我能全自动完成部署）

当前连接器读取实例信息正常，但执行命令被拒，说明所用凭据**缺少命令执行相关权限**。在腾讯云控制台：

1. 进入 **访问管理（CAM）→ 用户 / 角色**，找到 WorkBuddy 连接器所用的凭据（子账号或角色）。
2. 附加策略二选一：
   - **省事**：`QcloudLighthouseFullAccess`（轻量应用服务器全读写）
   - **最小权限**：自定义策略，仅放行
     ```
     lighthouse:DescribeInstances
     lighthouse:DescribeFirewallRules
     lighthouse:CreateFirewallRules
     lighthouse:ExecuteCommand
     lighthouse:DescribeInvocationTasks
     lighthouse:DescribeCommands
     ```
3. 保存后告诉我，我重新发起一次命令执行验证（只跑 `cat /etc/os-release` 这种只读命令）。

### 方案 B：给我 SSH 通道（最快，不涉及云权限）

服务器 22 端口已对全网开放，本机有 OpenSSH 客户端。你只需之一：

- 把**私钥文件**放到本机（例如 `C:\Users\Administrator\.ssh\dc_tencent.pem`），告诉我路径与登录用户名（Ubuntu 镜像通常是 `ubuntu`）
- 或者告诉我**登录密码**（同样建议部署后修改）

我就可以用 `ssh` / `scp` 完成装环境、传代码、起服务、配反代的全部动作。

> **建议**：先走方案 B 把服务跑起来（今天就能用），再慢慢配方案 A 的 CAM 权限。

### 顺带一个安全提醒

该机 **3306 端口当前对 `0.0.0.0/0` 全网开放**。如果 MySQL 只给本机应用用，建议加规则收紧为仅 `127.0.0.1` / 内网网段；公网暴露的数据库端口是暴力破解与入侵的首要目标。

---

## 三、没有域名，HTTPS 怎么办（回答你的第 2 问）

公网传输登录密码**必须加密**，否则同一网络中的任何人都可能截获。没有域名时只有三条路：

| 方案 | 用户体验 | 安全 | 建议 |
|---|---|---|---|
| **① IP + HTTP（明文）** | 直接访问 `http://81.70.230.116` | ❌ 密码明文过公网 | 仅限短期内部试用 |
| **② IP + 自签证书** | 访问 `https://81.70.230.116`，浏览器**每次弹"不安全"警告**，需手动点"继续" | ✅ 传输加密 | 可用，但每个同事都要教一遍 |
| **③ 申请域名 + 备案** | `https://xxx.com` 正常绿锁 | ✅ | **推荐**，但国内 80/443 对外服务需 ICP 备案（3–20 天） |

**我的建议（按顺序执行）**：
1. **今天**：先用方案 ① 把功能跑通给内部小范围看（明确告知"这是试用，别用重要密码"）。
2. **同时**：在腾讯云申请一个便宜域名（.top/.xyz 常见活动价）+ 提交免费 ICP 备案。
3. **备案通过后**：切方案 ③，用腾讯云免费 DV 证书，80→443 跳转，登录模块正式对外开放注册。

> 若你能接受"每个同事点一次浏览器警告"，方案 ② 也可以立刻上线，不需要等备案。

### ✅ 已选定（2026-09-22）：方案 ②「IP + 自签证书」，不走备案

证书已在服务器上生成完毕：

| 项 | 值 |
|---|---|
| 证书 / 私钥 | `/opt/dc/certs/dc.crt`（0644） / `/opt/dc/certs/dc.key`（**0600**） |
| 有效期 | **10 年**（3650 天） |
| 主体 | `C=CN, ST=Beijing, O=DataConverter, CN=81.70.230.116` |
| SAN | **`IP Address:81.70.230.116`**（必须有这一项，否则浏览器报证书不匹配） |
| 最终访问地址 | `https://81.70.230.116`（443 端口） |

**同事第一次访问会看到"您的连接不是私密连接"**——点「高级」→「继续前往 81.70.230.116（不安全）」即可，
之后该浏览器不再提示。传输内容是**加密**的（自签证书只解决"信任链"问题，不解决加密强度）。

> 生成命令留档（复现用）：
> ```bash
> openssl req -x509 -nodes -newkey rsa:2048 -days 3650 \
>   -keyout /opt/dc/certs/dc.key -out /opt/dc/certs/dc.crt \
>   -subj '/C=CN/ST=Beijing/O=DataConverter/CN=81.70.230.116' \
>   -addext 'subjectAltName=IP:81.70.230.116'
> ```

---

## 四、服务器 MySQL 情况确认

该机 3306 已放通，但**我目前无法登录服务器确认是否已安装 MySQL**。两种可能：

- **已安装**：需提供 root 密码或一个专用账号（我会建一个只给本应用用的库与账号）
- **未安装**：部署时一并安装（推荐用 Docker 起 MySQL 8，隔离干净、易于备份）

无论哪种，部署时都会建：

```sql
create database if not exists dc_cloud character set utf8mb4 collate utf8mb4_0900_ai_ci;
create user if not exists 'dc_app'@'127.0.0.1' identified by '<部署时生成>';
grant all privileges on dc_cloud.* to 'dc_app'@'127.0.0.1';
flush privileges;
```

> **不要**让应用直接用 root 连库——应用层会执行用户上传的 SQL（查询模块已放开写权限），
> 用独立账号 + 单库授权可以把爆炸半径限制在这个库里。

---

## 五、部署后你会得到什么

| 项 | 内容 |
|---|---|
| 访问地址 | `http://81.70.230.116`（备案后升级为 `https://你的域名`） |
| 登录 | 首次访问进入登录页；管理员账号由引导流程创建 |
| 数据目录 | 服务器 `/opt/dc/runtime`（`imports.db`、上传、导出、密钥文件） |
| 进程管理 | Docker 容器或 systemd 服务，开机自启 |
| 反代 | Nginx 监听 80/443，转发到 `127.0.0.1:51978`（服务本身不暴露公网） |
| 备份 | `/opt/dc/runtime` 定时打包（可用项目自带 `scripts/create_data_backup.py`） |

---

## 六、需要新增的防火墙规则（确认后我来加）

| 端口 | 用途 | 现状 |
|---|---|---|
| 443 | HTTPS | ❌ 需新增 |
| 80 | HTTP / ACME 证书校验 | ✅ 已有 |
| 22 | SSH | ✅ 已有（建议收紧来源 IP） |
| 3306 | MySQL | ⚠️ 已有但**建议关闭对外**（仅本机使用） |

---

## 七、域名与备案操作指引（2026-09-22 补充）

### 8.1 先做这一步：服务器续费（否则备案直接卡住）

该机 **2026-10-19 到期，只剩约 4 周**。而 ICP 备案要求备案所用云资源**剩余时长 ≥ 3 个月**
（部分省管局要求更长）——**不续费就无法提交备案**。
→ 建议直接续 1 年（也避免服务中途到期停摆）。续费后告诉我，我复核实例信息。

### 8.2 两条路线（**推荐先走 A，别为备案干等**）

| 路线 | 做法 | 拿到 HTTPS 的时间 | 访问地址形态 |
|---|---|---|---|
| **A. 不备案先用** | 域名 + 免费 DV 证书 + **改用 8443 等非 80/443 端口** | 域名注册+实名后**当天** | `https://你的域名:8443` |
| B. 备案 | A 的基础上补 ICP 备案，之后可正常用 80/443 | 备案 7–20 个工作日 | `https://你的域名` |

> 关键事实：国内未备案域名**只阻断 80/443**，其他端口不受影响。面向同事的内部工具用 8443 完全够用，
> 备案可以并行推进，不必等。

### 8.3 第一步：注册域名并实名（约 10 分钟 + 1–2 天审核）

1. 腾讯云控制台 → **域名注册** → 选 `.com` / `.cn` / `.top`（首年常见 1–30 元）。
2. 提交订单后立即做**实名认证**：企业用营业执照，个人用身份证。
3. 实名审核 **1–2 个工作日**；通过后才能提交备案（备案要求域名持有人与备案主体一致）。

### 8.4 第二步：备案（微信小程序，全程线上）

**要准备的材料**

| 材料 | 说明 |
|---|---|
| 营业执照 | 新鲜照片，在有效期内 |
| 法人身份证 | 正反面 |
| 网站负责人身份证 | 正反面；负责人可非法人（需授权） |
| 法人手机号 + 负责人手机号 | **两个不同号码**，用于核验电话 |
| 邮箱 | 接收备案通知 |
| 公章 | 《备案真实性核验单》打印后签字盖章 |
| 服务器 | 已续费、剩余时长 ≥3 个月的轻量应用服务器（就是这台） |

**流程**

1. 微信扫「腾讯云备案」小程序码 → 登录腾讯云账号。
2. **验证备案类型**：备案地区=营业执照注册地省份；类型=单位备案；填写域名；资源类型选**轻量应用服务器**并选中本机。
3. 填**主体信息**（公司全称、统一社会信用代码、法人姓名/证件号/手机号、注册地址）。
4. 填**网站负责人信息**（手机号不能与法人同号）。
5. 填**网站信息**：网站名称建议「公司简称 + 内部工具」，服务内容选「企业展示」类，前置审批项全部选「以上都不涉及」。
6. 上传材料 + 微信扫码完成**负责人视频核验**（人脸）。
7. 提交 → **腾讯云初审 1–2 个工作日**（专员会打负责人电话核实，务必接听）。
8. **短信核验（最容易翻车的一步）**：收到 **12381** 短信后，必须在 **24 小时内**点链接完成核验，**超时订单作废**。
9. **管局终审 7–20 个工作日** → 通过后收到含 ICP 备案号的短信/邮件。

**备案通过后**：30 天内到「全国互联网安全管理服务平台」做**公安备案**（免费，线上提交即可）。

### 8.5 建议的时间安排

| 时间 | 动作 |
|---|---|
| 今天 | ① 服务器续费 1 年　② 注册域名并提交实名 |
| 1–2 天后 | 实名通过 → 我配 8443 + 免费证书 → **同事当天就能用 HTTPS 访问** |
| 同时 | 提交备案申请（材料齐的话当天可提交） |
| 2–4 周后 | 备案通过 → 我加 80/443 防火墙规则与跳转 → 地址变成干净的 `https://你的域名` |

---

## 八、权限问题的实测澄清（对应你的截图）

你截图里「访问管理 → 用户列表」**只有主账号**（账号 ID `100052287782`，无子用户），
说明连接器用的是主账号凭据——**腾讯云 CAM 侧权限本来就是满的**，问题不在那里。

**放开连接器授权后已实测（2026-09-22 10:41）**，命令执行通道打通，但平台对**命令内容**有安全过滤：

| 类型 | 实测结果 |
|---|---|
| 只读信息（`cat` `free` `df` `uptime` `ss` `whoami`） | ✅ 可执行 |
| 写文件、建目录（`echo >` `mkdir`） | ✅ 可执行 |
| 装软件（`apt-get install`） | ✅ 可执行（已装 nginx 1.28.3） |
| 生成证书（`openssl`） | ✅ 可执行 |
| **含口令的命令**（如 `PASSWORD=xxx`） | ❌ **被拦（凭据泄漏防护）** |
| **`sudo` 提权** | ❌ 被拦（**但不需要——命令默认以 root 执行，uid=0**） |
| **`mysql` 客户端命令** | ❌ **被拦** → 数据库初始化无法走 API 通道 |

> 结论：**应用层部署可以由我全程完成**（装环境、拉代码、配 nginx、起服务、改防火墙）；
> **唯独"建库建账号"这一步走不通**——平台拦掉了 `mysql` 命令，而它又必须带 root 口令。

### 8.1 数据库初始化：请你在服务器上执行（1 分钟）

SSH 登录服务器（或在轻量控制台点「登录」进终端），执行：

```sql
-- 1) 建库（与公司内网库完全隔离的独立库）
create database if not exists dc_cloud character set utf8mb4 collate utf8mb4_general_ci;

-- 2) 建应用专用账号（不要用 root，密码请自己生成一个强口令）
create user if not exists 'dc_app'@'127.0.0.1' identified by '在这里填一个强口令';
grant all privileges on dc_cloud.* to 'dc_app'@'127.0.0.1';
flush privileges;
```

> 口令**不要发给我**，直接写进服务器上的 `/opt/dc/app/.env` 即可（我下一步会告诉你文件格式）。
> 之后我会用 `127.0.0.1` + 该账号把应用接上去。

### 8.2 部署完成记录（2026-09-22 11:10）

| 项 | 值 |
|---|---|
| **访问地址** | **`https://81.70.230.116`**（HTTP 80 自动 301 跳转） |
| 应用版本 | **1.5.0** |
| 进程管理 | systemd 服务 `data-converter`（开机自启）；日志 `/opt/dc/runtime/logs/service.log` |
| 反向代理 | Nginx 1.28.3：443 TLS → `127.0.0.1:51978` |
| 证书 | `/opt/dc/certs/dc.crt`（自签，10 年，SAN = IP） |
| 代码 / 运行环境 | `/opt/dc/app` · `/opt/dc/venv`（Python 3.14.4） |
| 数据目录 | `/opt/dc/runtime/{data,uploads,exports}` |
| 配置 | `/opt/dc/app/.env`（权限 600，含随机 `DC_MASTER_KEY`） |
| 数据库 | 本机 MySQL，库 `dc_cloud`，账号 `dc_app@127.0.0.1` |

一键脚本：`deploy/tencent-cloud-setup.sh`（幂等，可重复执行；本次由用户在服务器终端运行）。

### 8.3 ⚠️ 部署中发现并已修复：认证曾完全未生效

**问题**：`server.py:public_auth_enabled()` 要求 `APP_AUTH_ENABLED` 为真**且** `ADMIN_PASSWORD` 非空
（两个条件同时满足才算开启）。一键脚本只写了前者 → 认证被判定为"未启用" →
**公网任何人可直接访问，无需登录**。

**实测证据**（修复前，从外部无凭据访问）：

```
GET https://81.70.230.116/api/connections  →  200 {"ok": true, "connections": []}
```

**修复**：生成随机强口令写入 `.env` 的 `ADMIN_PASSWORD` 并重启服务。
**修复后实测**：

```
GET https://81.70.230.116/                 →  401 Authentication required.
GET https://81.70.230.116/api/connections  →  401 Authentication required.
带 Basic 凭据访问以上两个地址                →  200
```

> **给后续部署的教训**：`APP_AUTH_ENABLED=true` 单独设置**不等于**开启认证。
> 脚本已按此修正（生成随机 `ADMIN_PASSWORD`），但**任何新环境上线后都必须做一次
> 「无凭据访问应返回 401」的外部验证**，不要假定配置生效。

### 8.4 当前登录方式（Basic，临时挡板）

账号 `admin`，口令为脚本生成并保存在服务器 `/opt/dc/app/.env`（**不在本文档与仓库中出现**）。
查看方式：服务器上执行 `grep '^ADMIN' /opt/dc/app/.env`。

> 这是**临时挡板**：单账号、无角色、无审计、无登出、口令靠明文环境变量。
> 正式方案仍是《登录功能-实施设计》里的 P1–P4；本期先堵住匿名访问这道口子。

### 8.5 遗留待办

| # | 事项 | 优先级 |
|---|---|---|
| 1 | `dc_app` 口令为弱口令且已出现在聊天记录中 → **立即改强** | 高 |
| 2 | 3306 / 33060 对全网开放 → 建议关闭对外（应用只需 `127.0.0.1`） | 高 |
| 3 | 实施 P1–P4 登录模块，替换 Basic 认证 | 中 |
| 4 | 在 UI 里建第一个连接：`127.0.0.1` / `dc_cloud` / `dc_app` | 中 |
| 5 | 服务器 2026-10-19 到期 → 续费 | 中 |

---

## 九、下一步

1. 你选第二节的方案（**推荐方案 B：给我 SSH**；或在连接器设置里放开命令执行）。
2. 告诉我服务器上 MySQL 是否已装（第四节）。
3. HTTPS 走 ①→③ 的渐进路线，还是直接 ②？
4. 以上定了之后，我这边**立即开始 P1（认证内核）编码**——这部分不依赖服务器，可以先做完再部署。
