# 数据导表工具 —— 测试报告与改进计划（交付文档）

> **文档目的**：本文档是 2026-09-06 对 `data-converter-tool` 全量黑盒测试 + 代码走查的完整产出。
> 读者可以是任何人或任何 AI Agent：每个问题都包含【现象 → 复现步骤 → 根因 → 修复建议 → 验收标准】，
> 无需额外上下文即可理解并动手修复。
>
> **重要约束**：本文档只是测试结论与建议，**未改动项目任何代码**。所有修复需经项目所有者确认范围后执行。

---

## 0. 项目与测试环境速览

| 项 | 值 |
|----|----|
| 被测项目 | https://github.com/QiGuaiJun/data-converter-tool （本地副本：本目录） |
| 项目形态 | Python 单文件后端 `server.py`（约 3600 行）+ `public/` 原生 HTML/JS 前端；本地 SQLite（`data/imports.db`）存配置 |
| 定位 | 桌面风格的数据导表工具：Excel↔MySQL 导入导出、查询、作业、定时任务 |
| 测试时间 | 2026-09-06（项目 main 分支，41 个提交，最新提交 fd8f1ab） |
| 测试方式 | 真实浏览器黑盒操作 + 服务端逐格数据比对 + 针对性代码走查 |

**测试环境（已搭建，可直接复用）**：

- Python 3.12.10 虚拟环境：`data-converter-tool/.venv/`（依赖已按 `requirements.txt` 装好）
- 启动服务：`set PORT=51978 && .venv\Scripts\python.exe server.py` → http://127.0.0.1:51978/
- 一次性测试 MySQL 8.0.28：端口 **3307**，数据目录 `../test-mysql/`（配置 `../test-mysql/my.ini`），账号 `tester/test123456`，测试库 `dc_test`。启动命令：
  `D:\mysql\mysql-8.0.28-winx64\bin\mysqld.exe --defaults-file=<test-mysql>\my.ini --console`
  ⚠️ 机器上另有所有者本人的 MySQL 服务（3306 端口），**严禁触碰**。
- 测试资产目录 `../testdata/`：
  - `generate_test_files.py` / `generate_small_feature_files.py` —— 生成全部测试文件
  - `ground_truth.json` —— 每个测试文件的逐格基准数据
  - `compare_import.py` / `compare_export.py` —— 导入/导出结果与基准的逐格比对脚本（运行：`.venv\Scripts\python.exe ..\testdata\compare_import.py`）
  - 测试文件：01~12 号（基础/边界/特殊字符/多Sheet/2万行/重复行/精度/空值/CSV/坏文件）、20~24 号（行控制/清洗/中文表头/分号CSV/英文CSV）

---

## 1. 严重级别定义

- **P1**：数据错误/丢失、核心功能失效、安全问题 —— 必须修
- **P2**：功能开关失效、明显体验缺陷、有缓解措施的安全问题 —— 应该修
- **P3**：文案、细节、打磨项 —— 建议修

---

## 2. Bug 清单（共 28 项）

### P1-1 日期列永远导入成 text，「自动识别数字及日期类型」对日期无效 ✅ 已修复（2026-09-06）

- **修复内容**：`server.py` 新增 `parse_date_text()` / `convert_date_columns()`；导入流程在 `build_target_data` 之后把整列均为日期形态的文本还原为 `date/datetime` 对象（仅 MySQL 目标、"自动识别"模式生效；SQLite 行为不变）；`infer_column_types()` 增加日期实例分支——全 date → `date`，含 datetime → `datetime`。纯日期列建 `date`，混合日期+时间的列建 `datetime`。
- **验证结果**：01 号文件 `入职日期` → `date` 类型，值为 `2021-02-02`（无 00:00:00 后缀）；02 号文件混合列 → `datetime`、空值保持 NULL；05 号 2 万行 → `datetime`，逐格比对 PASS；SQLite 目标与 CSV 导入行为不变，全量回归无新差异。
- **原问题描述**（保留供参考）：

- **现象**：Excel 中的日期列（如 `入职日期`，值为 `2021-02-02`）导入 MySQL 后列类型是 `text`，值变成 `2021-02-02 00:00:00`。
- **复现**：导入 `testdata/01_基础50行.xlsx`（导入模式=重建、数据类型=自动识别数字及日期类型）→ `DESCRIBE t_01_basic` → `入职日期 text`。
- **根因**：`server.py:1136` `infer_column_types()` 只有三类判断：全整数→bigint、全部匹配 `[-+]?(\d+(\.\d*)?|\.\d+)`→double、其余→text。**没有任何日期识别逻辑**。openpyxl 读出的日期对象被 `str()` 成 `2021-02-02 00:00:00` 后落入 text 分支。
- **修复建议**：
  1. 在 `infer_column_types` 中增加日期检测：值全部能被 `datetime.strptime` 常见格式（`%Y-%m-%d`、`%Y/%m/%d`、`%Y-%m-%d %H:%M:%S`、`%Y/%m/%d %H:%M` 等）解析时，MySQL 目标建 `datetime`（纯日期可建 `date`）；
  2. 日期值以 `datetime` 对象传给驱动（不要先 str 化），让 MySQL 存成真正的日期类型；
  3. 仅含日期部分时输出格式不再追加 `00:00:00`。
- **验收标准**：重新导入 01 号文件后 `DESCRIBE` 显示 `入职日期 date/datetime`，`SELECT` 返回 `2021-02-02`；`compare_import.py` 对该列比对通过（基准为 `2021-02-02`，允许 date/datetime 类型）。

### P1-2 导入成功后「已导入表/表数据」面板永远显示 0 张表/表不存在

- **现象**：向 MySQL 导入成功（日志显示"写入 50 行成功"），左侧「已导入表」显示 `0 张表`；点击日志里的表名提示"表不存在或没有字段"。用户会误判导入失败。
- **根因**：`server.py:3247` `handle_tables()` 与 `handle_table_preview()` 查询的是**应用自身配置库 SQLite 的 `sqlite_master`**（`connect_db()`），不是用户选择的 MySQL 目标连接。该面板只对"本地 SQLite"目标有意义，对 MySQL 目标永远为空/报错。
- **修复建议**：
  1. 短期：目标为 MySQL 时，该面板改用当前所选连接（connectionId）查询 `information_schema.tables`（或直接隐藏面板并提示"MySQL 目标请在【表】页面查看"）；
  2. 长期：「表」页面已具备完整能力，导入页面板可改为导入结果的最近 N 条摘要（表名+行数+时间）。
- **验收标准**：向 MySQL 导入成功后，导入页能看到刚导入的表名且点击预览不报错；断开目标库时给出友好提示而非"表不存在"。

### P1-3 前端硬编码凭据与作者环境残留，密码随 GET URL 明文传输并写入服务器日志

- **现象**：服务器日志可见 `GET /api/target-tables?...&dbPassword=123456&...`。
- **根因（多处）**：
  - `public/index.html:98`：`<input id="dbPassword" type="hidden" value="123456" />`（硬编码密码）
  - `public/index.html:94`：`<input id="dbHost" type="hidden" value="192.168.1.102" />`（作者内网 IP）
  - `public/app.js:633-637`：新建连接对话框默认值 `192.168.1.102`、密码兜底 `"123456"`、默认库 `"lcdp_SR"`（作者的库名）
  - `public/connections.html:58`、`public/connections.js:60`：主机默认 `192.168.1.102`
  - `public/app.js:711-715` `connectionParams()`：把 dbPassword 放进 **GET 查询串**（服务器日志留痕）
- **修复建议**：
  1. 删除全部硬编码默认值（主机默认 `127.0.0.1`、无密码兜底、无默认库名）；
  2. 涉及连接信息的接口改为 **POST + body** 传递凭据，禁止 GET query 传密码；服务端日志脱敏；
  3. 目标连接已保存（有 connectionId）时，前端不重复传凭据，服务端用存储的连接信息（现在部分接口已支持，需统一）。
- **验收标准**：全新环境启动后所有表单无预填密码/IP；服务器日志中 grep 不到 `dbPassword=`；连接与导入导出全流程功能不变。

### P1-4 数据库密码仅 Base64 编码存储，等同明文

- **现象/根因**：`server.py:64` `PASSWORD_PREFIX="b64:"`、`server.py:234` `encode_secret()` 仅做 `base64.b64encode`。拿到 `data/imports.db`（或 README 指导生成的备份 zip）即可还原全部数据库密码。依赖中已有 `cryptography` 但未使用。
- **修复建议**：
  1. 用 `cryptography.fernet` 对称加密密码列；密钥派生自本机机器指纹或用户设置的**主密码**（推荐主密码方案：首次使用设置主密码，读取连接时解锁）；
  2. 提供 `scripts/migrate_password_encryption.py` 兼容旧 b64 数据自动升级；
  3. `create_data_backup.py` 导出的备份包提示含加密数据，主密码不随包携带。
- **验收标准**：SQLite 中 password 列不再出现 `b64:` 明文可逆格式；导入/导出/测试连接功能正常；忘记主密码时有明确的重置指引（重置=清除已存密码）。

### P1-5 「按批次分割导出文件」静默丢弃数据（高危）

- **现象**：导出 50 行，勾选"按批次分割导出文件"+每批 20 行 → 只生成 **1 个文件、仅前 20 行**，后 30 行丢失，界面提示导出成功。
- **根因**：`server.py:1662` `apply_export_batch_limit()` 实现 `return rows[:batch_rows]` —— 只截取前 N 行，**没有任何分割多文件的逻辑**；流式路径（`server.py:1971-1990`）同样是 `rows_seen >= batch_limit` 后 break。
- **修复建议**：实现真正的分割：按每批 N 行生成 `文件名_001/002/003...`；或短期先改 UI 文案为"限制导出行数"并加二次确认提示"超出部分将被丢弃"，绝不能静默截断。
- **验收标准**：50 行、每批 20 → 生成 3 个文件（20/20/10 行），总和=50；大表（2 万行、每批 5000）同样成立；`compare_export.py` 思路的比对通过。

### P2-6 含科学计数法/超大数的列整列降级为 text 且精度损失

- **现象**：`testdata/07_数值精度.xlsx` 中含 `1e15`、`123456789012345678` 的列全部建成 `text`；`123456789012345678` 入库后变成字符串 `1.234567890123457e+17`（精度丢失）。
- **根因**：`infer_column_types` 的小数正则不认科学计数法（含 `e` 的值落入 text 分支）；text 路径的 str 化用默认浮点格式。
- **修复建议**：正则扩展支持 `[-+]?\d*\.?\d+([eE][-+]?\d+)?`；或检测到科学计数值时按 float 处理建 `double`；大整数优先按整数处理；字符串化使用 `repr`/定点格式避免精度损失。
- **验收标准**：07 号文件各列类型合理（全数值列不再 text）；值逐格比对通过（允许 double 精度内差异，但不得出现 `e+17` 式字符串）。

### P2-7 「表」页与导出对象列表的行数是 InnoDB 估算值

- **现象**：`t_05_large` 真实 20000 行，页面显示 20,137 行。
- **根因**：行数取自 `information_schema.TABLES.TABLE_ROWS`（InnoDB 估算值）。
- **修复建议**：行数 ≤ 10 万时用 `SELECT COUNT(*)` 精确值；更大时仍用估算值但界面标注"约"。
- **验收标准**：对 `t_05_large` 显示 20000（或明确标注"约"）。

### P2-8 导出时目标文件夹不存在则直接失败，不自动创建

- **现象**：导出到不存在的文件夹 → HTTP 400「目标文件夹不存在」。
- **修复建议**：导出前 `Path(folder).mkdir(parents=True, exist_ok=True)`；创建失败再报错。导入侧目录创建逻辑可参考。
- **验收标准**：导出到全新多层目录成功。

### P2-9 定时任务 running 标志无启动恢复，崩溃后调度永久卡死

- **现象/根因**：`server.py:2648-2685`：调度执行前置 `running=1`，结束时置 0；**进程在执行中崩溃/被杀后无任何启动清理**，`dispatch_due_schedules` 永远过滤 `running=0`，该调度不再触发。
- **修复建议**：服务启动时执行 `UPDATE _schedules SET running = 0 WHERE running = 1;`（可选：同时记一条"上次异常中断"日志）。
- **验收标准**：手工把某调度 running 置 1 后重启服务，该调度恢复正常调度。

### P2-10 「自动主键」（autoPkField）功能报错不可用

- **现象**：导入时指定自动主键列 `编号` → `(1060, "Duplicate column name '编号'")`，导入失败。
- **根因**：主键处理逻辑在目标表已有同名列时仍尝试新增该列，而不是把现有列设为 PRIMARY KEY。（实现在 `target_create_or_expand_table` 附近，未逐行定位。）
- **修复建议**：若 autoPkField 已存在于列清单，建表时直接在该列定义后追加 `PRIMARY KEY`（或 `ALTER TABLE ... ADD PRIMARY KEY`）；只有不存在时才新增自增列。
- **验收标准**：对含 `编号` 列的文件启用自动主键，导入成功且 `DESCRIBE` 中该列 `Key=PRI`。

### P2-11 「指定日期列」（dateColumns）手动强制日期类型不生效

- **现象**：导入时 dateColumns 填 `入职日期` → 建表仍为 `text`。
- **根因**：待定位（前端字段 id `dateColumns` 已正确提交；服务端处理链路未逐行确认）。与 P1-1 同属日期类型问题，修复 P1-1 时应一并排查该字段是否被消费。
- **验收标准**：dateColumns 指定的列建成 `date/datetime` 类型。

### P2-12 「跳过自上次导入后未曾更新过的文件」是死开关

- **现象**：同一文件同配置连续导入 3 次，3 次都全量重导（rows=6/6/6，skipped=0/0/0）。
- **根因**：`server.py` 全文无 `skipSeenFile` 的处理逻辑（前端提交了，后端没实现）。
- **修复建议**：实现文件指纹记录（路径+mtime+size 存 SQLite），命中且勾选跳过时 rowsSkipped 计数并写日志"文件未变更，已跳过"；或先在 UI 移除该选项避免误导。
- **验收标准**：第二次导入同文件时日志显示跳过且无写入。

### P2-13 「如果表存在注释，使用表注释作为文件名」是死开关

- **现象**：`ALTER TABLE job_t_06 COMMENT='重复行验证表'` 后导出，文件名不使用注释。
- **根因**：`server.py` 无 `commentAsFileName` 引用。
- **修复建议**：实现：导出对象为表时读取表注释，若非空且选项开启，用注释作文件名（注意文件名非法字符清洗）；或移除该选项。
- **验收标准**：带注释表导出文件名为注释文本。

### P2-14 `/api/export/download?path=...` 不校验路径，可下载服务器任意文件

- **现象**：`GET /api/export/download?path=C:\Windows\win.ini` 返回 200 + 文件内容；`imports.db`（含全部连接密码）同样可下载。
- **缓解因素**：该接口在 `require_auth()` 之后（云端受管理密码保护）；响应无 CORS 头（恶意网页无法跨域读取）。但本地模式无认证 + 路径不校验仍是实际风险。
- **修复建议**：下载仅允许 `exports/`、`uploads/` 白名单目录内的文件（realpath 校验前缀），或改为"下载令牌"机制（导出时生成一次性 token，禁止 path 直传）。
- **验收标准**：传入白名单外路径返回 403/400；正常导出文件的下载不受影响。

### P2-15 重建/覆盖模式确认框后，「开始导入/预览」按钮永久禁用

- **现象**：选择"重建"模式点击开始导入 → 弹原生 confirm；确认框被关闭（无论取消/接受）后两个按钮保持 disabled，需刷新页面。
- **根因**：`public/app.js:853-881` `importFiles()` 禁用按钮后依赖 try/finally 恢复；confirm 阻塞路径上状态未恢复（在自动化/特殊 webview 环境下必现；普通浏览器待复测）。
- **修复建议**：用自定义模态替代 `window.confirm`（项目已有 dialog 风格）；按钮恢复逻辑改为"动作结束必恢复"，并加 `window.onbeforeunload` 之外的兜底。
- **验收标准**：确认/取消任意路径下按钮恢复可用；真实浏览器与桌面壳各验证一次。

### P2-16 导入/导出编辑器刷新即丢失全部配置

- **现象**：填了十几项配置未点"保存为任务"，刷新页面全部清空（无草稿）。
- **修复建议**：编辑器状态实时写入 `localStorage`（按模块 key），下次打开提示恢复草稿；或离开页面前 `beforeunload` 提示。
- **验收标准**：配置→刷新→恢复；正式保存任务后草稿清除。

### P3-17 坏文件导入报英文原文

- **现象**：导入伪 xlsx → `File is not a zip file`。
- **修复建议**：捕获解析异常映射为中文文案："文件损坏或不是有效的 Excel/CSV 文件"。

### P3-18 数据库断连等错误直接展示原始驱动异常

- **现象**：停掉 MySQL 后查询显示 `(2003, "Can't connect to MySQL server on '127.0.0.1' ([WinError 10061]...)")`。
- **修复建议**：统一错误翻译层：连接类异常 → "无法连接数据库，请检查主机/端口/账号或网络"；保留"查看详情"折叠原始信息。

### P3-19 数据库下拉列出系统库干扰选择

- **现象**：连接测试后数据库下拉含 `information_schema/mysql/performance_schema/sys`。
- **修复建议**：过滤 MySQL 系统库（可加"显示系统库"开关）。

### P3-20 默认数据转换无提示（空串→NULL、首尾空格裁剪）

- **现象**：`''` 导入成 NULL；`' 前后空格 '` 变 `'前后空格'`。选项存在但默认开启且无说明。
- **修复建议**：导入预览页展示"将应用的清洗规则"清单；文档说明默认行为。

### P3-21 四个导航入口是占位页（同步/API/操作手册/咨询反馈）

- **现象**：四个页面仅显示"此模块已拆分…功能开发时接入"。
- **修复建议**：占位页标注"开发中"角标，或先从导航移除；**操作手册建议优先补**（新用户唯一文档入口）。

### P3-22 任务保存后列表不自动刷新；打开任务时文件路径不回显

- **现象**：①"保存为任务"成功后任务列表无新条目（刷新后才出现）；②"打开导入"后可见的"文件路径"框为空（路径实际保存在隐藏字段 `#importTaskPath`，直接点开始导入能成功）——用户会误以为要重选文件。
- **修复建议**：保存成功后调用 loadJobs()；打开任务时把隐藏路径填回可见输入框。

### P3-23 任务/作业/查询/调度混合显示，语义不清

- **现象**：作业列表混入"保存的查询"（如 `查询：部门人数统计`）和"定时任务自动作业"；导入页任务列表混入普通作业。
- **修复建议**：列表按类型分组或加类型徽标；侧栏聚合展示时明确标注类型。

### P3-24 背景壁纸导致文字对比度不足

- **现象**：动漫壁纸+半透明卡片，页面右上角白色标题在浅色天空背景上几乎不可读（截图已确认）。
- **修复建议**：提供"素色/低透明度"外观开关；至少提高卡片不透明度到 0.9+。

### P3-25 测试连接/导入执行期间页面疑似同步阻塞

- **现象**：点击「测试连接」等按钮时 Playwright 点击超时（页面主线程冻结数秒），疑似存在同步 XHR。
- **修复建议**：grep 前端确认无同步请求；按钮加 loading 态防重复点击。

### P3-26 favicon 404

- **现象**：每次刷新服务器日志刷一条 404。加一个 favicon。

### P3-27 导入时间列（importTimeField）建成 text 类型

- **现象**：`imported_at text`，值为 `2026-09-06 20:48:35` 字符串。建议建 `datetime` 并用 `NOW()` 由数据库填充（可选）。

### P3-28 界面无版本号/更新日志入口

- **修复建议**：页脚或"关于"显示版本号（可取 git commit 短 hash），链接到变更日志。

---

## 3. 产品改进建议（按用户价值排序）

### A. 导入预览增加「目标表结构预览 + 类型预警」（配合 P1-1/P2-6）

- **问题**：用户在预览时只看到数据行，看不到每列将被识别成什么类型；类型猜错（日期变 text、大数变科学计数法）只有事后查库才发现。
- **方案**：预览响应中附带 `infer_column_types` 的结果，字段映射对话框增加"目标类型"列；无法确定类型的列标 ⚠️ 并给出原因（"包含无法解析的值：第 3 行 xxx"）；允许用户在预览里手动改列类型。
- **验收**：导入前即可看到并将日期列改为 date；导入后 DESCRIBE 与预览一致。

### B. 作业/定时任务的导入步骤支持完整导入配置

- **问题**：作业编辑器的导入步骤只有 路径/目标表/导入模式 三个字段，交互页面里的字段匹配、类型识别、清洗规则等配置在作业里全部走样——用户以为"保存的任务=完整复现"，实际不是（结构性缺陷，测试确认）。
- **方案**：作业步骤引用"已保存的导入任务"（id 引用而非复制简化配置）；或步骤编辑器复用完整导入表单。
- **验收**：按交互页配置的任务经作业执行后，结果与手动导入逐格一致。

### C. 导入结果明细报告（跳过/失败行）

- **问题**："跳过 N 行"不说是哪几行、为什么。
- **方案**：导入完成后可展开"问题行清单"（行号+原因+原始值），支持导出为 CSV；错误行不影响其余数据（现有行为保留）。
- **验收**：构造含坏值的文件，报告能定位到具体行号与原因。

### D. 新手引导 + 操作手册落地

- **方案**：首次启动显示 3 步引导（连接→导入→导出）；`docs.html` 至少覆盖：连接管理、导入全部选项说明（含默认行为 P3-20）、导出选项、作业与定时任务关系图、常见错误对照表。

### E. 关闭程序时提示定时任务将停用

- **问题**：定时任务仅在程序运行时生效，关闭程序后静默失效（用户核心痛点"定时不稳"的隐藏雷区）。
- **方案**：有已启用调度时，关闭窗口弹确认"N 个定时任务将停止，确定退出？"；调度页常驻说明文案。

### F. 危险操作保护统一

- 「成功后删除源文件」：运行时二次确认 + 优先移入回收站（`send2trash`）。
- 「重建/覆盖」确认改自定义模态（同时解决 P2-15）。

### G. 外观与版本（配合 P3-24/28）

- 素色模式开关；页脚版本号（git hash）+ 变更日志页面。

### H. 把回归测试固化（工程护栏）

- 本文档第 0 节的测试资产（测试文件生成脚本 + ground_truth + 比对脚本）整理进仓库 `tests/`；
- 每次让 AI 修改代码后必须跑：生成文件 → 启动服务 → 全量导入 → `compare_import.py` → 关键导出 → `compare_export.py`；
- 再逐步把浏览器黑盒流程转为 Playwright 脚本。
- **理由**：本项目由 AI 从 0 到 1 生成，已发现的"死开关"（界面有、后端没实现）是典型症状——没有回归测试的 AI 迭代等于赌博。

---

## 4. 已验证通过的功能基线（修复时不得破坏）

以下功能在本次测试中验证通过，修复后回归必须保持：

- **连接**：新建/测试连接（自动加载数据库列表）/保存/刷新持久化/编辑时密码留空=不修改/删除连接
- **导入**（逐格比对通过）：01 基础 50 行、05 两万行大文件、06 重复行、08 空列空行、09 UTF-8 BOM CSV、10 GBK CSV；多 Sheet 自动按"表名_Sheet名"建表；含 emoji/换行/引号/SQL 注入样串/HTML 的数据安全入库；表头行号/数据开始行/跳尾行/行数限制/列筛选/自定义分隔符；中文表名→拼音（销售数据表→xssjb）、字段→拼音（订单编号→ddbh）、金额(元)→je_y 符号清洗、字段名大小写、表名前后缀、固定值列、导入时间列、文本替换、向下填充、重复列名→列_2、空值替换、空值填 0、指定 Sheet、导入前后 SQL、成功后删除源文件
- **导出**（逐格比对通过）：查询导出 50 行、整表 2 万行（1.2s）、CSV/GBK 编码、无表头模式、WHERE 过滤（30 行精确匹配）、指定字段、文件名前后缀、按字段值分割（5 部门 5 文件）、跳过空表、浏览器下载链接
- **查询**：SELECT/聚合、保存查询/删除查询/选中回填 SQL、只读拦截（DROP 被拒且中文报错）、转到导出跳转
- **作业**：创建/手动执行/失败后继续（步骤1失败步骤2照跑）/详细运行日志
- **定时**：每分钟调度连续 6 次准点成功；每日/每周/每月/每 N 分钟/一次性 5 种模式 next-run 计算全部正确；一次性执行后自动停用；禁用立即生效
- **韧性**：MySQL 断连时查询/表页不崩溃（文案待优化）；伪 xlsx/空文件/不存在路径均 400 且服务不崩

---

## 5. 本轮未覆盖的测试项（后续轮次建议）

1. 「更新/追加」导入模式的数据合并正确性（需构造新旧数据集）
2. 断点续传（resumeImport）、加密 Excel（本地难生成样本）、目录批量导入+遍历子目录
3. 作业步骤排序（↑↓）、作业嵌套作业、查询子任务执行
4. 导出格式选项（字号/行高/锁列/边框/锁表头）
5. 云端部署链路：ADMIN 认证全流程、Docker 镜像、Render/Railway 实际部署
6. Windows 桌面打包：PyInstaller（DataConverterTool.spec）、desktop_launcher
7. 性能与长稳：10 万行级、并发、内存、调度长时间运行
8. Edge/Chrome 真实浏览器兼容性（本轮用 ZCode 内置浏览器）

---

## 6. 修复执行须知（给接手的 Agent）

1. **先和所有者确认修复范围**（本文档问题编号即沟通语言），一次一批，改完即回归。
2. 修复顺序建议：P1-5（丢数据）→ P1-1/P2-11（日期）→ P1-2（面板）→ P1-3/P1-4/P2-14（安全三件套）→ 其余 P2 → P3。
3. 每完成一项：跑第 4 节相关基线 + 对应验收标准，记录结果。
4. 环境启动见第 0 节；**不要动 3306 端口的 MySQL 服务和所有者的任何真实数据**。
5. 所有前端改动注意三个缓存查询串（styles.css/app.js 的 ?v=）需同步更新版本号。
